// Simple GA4 tracking script — inject with <Script> in root layout or use gtag.
// In production, load via Google Tag Manager or inject this script in layout.
export const GA4_ID = process.env.NEXT_PUBLIC_GA4_ID || '';
